<Window x:Class="MarchingSquares.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarchingSquares"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Black"
        KeyDown="Window_KeyDown"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        ContentRendered="Window_ContentRendered"
        Title="MainWindow" Height="500" Width="500">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MarchingSquares;component/Dictio/MainDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Canvas x:Name="canvas"/>

        <DockPanel Grid.Row="1" LastChildFill="False" HorizontalAlignment="Center">

            <TextBlock>
                <Run Text="Resolution: "/>
                <Run Text="{Binding Path=MainWindowModel.Rez, UpdateSourceTrigger=PropertyChanged}"/>
            </TextBlock>
            <Slider Style="{StaticResource CustomSlider}" Value="{Binding Path=MainWindowModel.Rez, UpdateSourceTrigger=PropertyChanged}" Minimum="10" Maximum="50"/>

            <TextBlock Text="Increment"/>
            <Slider Style="{StaticResource CustomSlider}"/>

            <TextBlock>
                <Run Text="Speed: "/>
                <Run Text="{Binding Path=Speed, UpdateSourceTrigger=PropertyChanged}"/>
            </TextBlock>
            <Slider Style="{StaticResource CustomSlider}" Value="{Binding Path=Speed, UpdateSourceTrigger=PropertyChanged}" Minimum="30" Maximum="1000"/>
        </DockPanel>
    </Grid>
</Window>
